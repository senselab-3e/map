var location = ["proposition for social dreaming: brazil", "quilts", "helsinki", "amsterdam"];

function sprill(array) {
  for (var c in array) {
    // var div = document.createElement('div');
    // div.className = "img";
    // containerdiv.appendChild(div)
    // var div2 = document.createElement('div');
    // div2.className = "img__content";
    // var myImage = new Image();
    createA(array[c], "#");
    console.log("succes");
    // div2.appendChild(myImage)
    // div.appendChild(div2)
    // plant.appendChild(div);
    // containerdiv.appendChild(plant);
    // main.appendChild(containerdiv)
    // document.body.appendChild(main)
  }
}
